# requirements.txt - Optimized for Vercel deployment
# Core API dependencies
fastapi==0.104.1
mangum==0.17.0
python-multipart==0.0.6
pydantic==2.5.0

# Essential utilities
python-dotenv==1.0.0
requests==2.31.0

# Firebase (lightweight)
firebase-admin==6.2.0

# Image processing (essential only)
pillow==10.1.0

# Inference SDK for Roboflow
inference-sdk==0.9.12

# Lightweight AI/Chat dependencies
openai==1.3.0

# Remove heavy dependencies that cause size issues:
# langchain, langgraph, wikipedia, d3, plotly, tensorflow
# These will be replaced with direct API calls